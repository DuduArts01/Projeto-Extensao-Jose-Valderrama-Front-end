import { useNavigate } from "react-router-dom"; // enables navigation between pages

const Home = () => {
    const navigate = useNavigate();

    return(
        <>
            hello
        </>
    );
}

export default Home;